security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        app_user_provider:
            entity:
                class: App\Entity\Utilisateur
                property: courriel
        api_user_provider:
            entity:
                class: App\Entity\Utilisateur
                property: apiToken

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        api:
            pattern: ^/api/
            stateless: true  # Pas de session pour les API
            provider: api_user_provider # Utiliser le provider pour les API qui est en haut
            custom_authenticators:
                - App\Security\ApiTokenAuthenticator
            lazy    : true # Charger le firewall uniquement si nécessaire

        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: app_login
                check_path: app_login
                username_parameter: _username  # Nom du champ email dans le formulaire
                password_parameter: _password  # Champ mot de passe
                enable_csrf: true
                csrf_token_id: authenticate
                default_target_path: app_profil  # route après connexion réussie
            logout:
                path: app_logout
                target: app_login

    access_control:
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/register, roles: PUBLIC_ACCESS }
        - { path: ^/, roles: ROLE_CONTRIB }
