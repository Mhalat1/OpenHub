lexik_jwt_authentication:
    # secret_key: '%env(resolve:JWT_SECRET_KEY)%'  # ← COMMENTEZ
    # public_key: '%env(resolve:JWT_PUBLIC_KEY)%'  # ← COMMENTEZ
    pass_phrase: '%env(JWT_PASSPHRASE)%'
    token_ttl: 3600